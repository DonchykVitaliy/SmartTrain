<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="SmartTrain.GoalPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SmartTrain"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Мій тренувальний графік" FontSize="28" FontWeight="Bold" Margin="0,0,0,20"/>

        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <GridView x:ConnectionId='21' Grid.Column="1" x:Name="WeekCalendar"                                                 >
                <GridView.ItemTemplate>
                    <DataTemplate>
                        <Border Width="100" Height="100" CornerRadius="8" 
                            Background="{Binding BackgroundColor}" 
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" 
                            Margin="5">
                            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                <SymbolIcon Symbol="Accept" Foreground="LightGreen" Visibility="{Binding CheckmarkVisibility}" Margin="0,0,0,5"/>
                                <TextBlock Text="{Binding DayName}" FontSize="18" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                                <TextBlock Text="{Binding Status}" FontSize="12" Foreground="Gray" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </GridView.ItemTemplate>
            </GridView>

            <Button x:ConnectionId='22' Grid.Column="2" x:Name="NextWeekBtn" Content="Наступний тиждень ➔" 
                    Visibility="Collapsed"                           
                    VerticalAlignment="Center" HorizontalAlignment="Left" Margin="20,0,0,0" 
                    Style="{StaticResource AccentButtonStyle}" Height="100"/>
        </Grid>

        <TextBlock x:ConnectionId='2' Grid.Row="2" x:Name="SelectedDayTitle" FontSize="20" FontWeight="SemiBold" Margin="0,30,0,10"/>

        <Grid Grid.Row="3">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <ListView x:ConnectionId='16' Grid.Row="0" x:Name="DayExercisesList" SelectionMode="None">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{Binding CardBackground}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,5,15,5">

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="16"/>
                                    <TextBlock Text="{Binding Description}" Foreground="Gray" TextWrapping="Wrap" Margin="0,5,0,10"/>

                                    <StackPanel Orientation="Horizontal" Spacing="20">
                                        <StackPanel Orientation="Horizontal" Spacing="5">
                                            <SymbolIcon Symbol="RepeatAll" Foreground="{ThemeResource SystemAccentColor}"/>
                                            <TextBlock Text="Підходів:"/>
                                            <TextBlock Text="{Binding Sets}" FontWeight="SemiBold"/>
                                        </StackPanel>

                                        <StackPanel Orientation="Horizontal" Spacing="5">
                                            <SymbolIcon Symbol="Clock" Foreground="{ThemeResource SystemAccentColor}"/>
                                            <TextBlock Text="Час:"/>
                                            <TextBlock Text="{Binding EstimatedTimeMinutes}" FontWeight="SemiBold"/>
                                            <TextBlock Text="хв"/>
                                        </StackPanel>
                                    </StackPanel>
                                </StackPanel>

                                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Spacing="10">
                                    <Button x:ConnectionId='19'                        Background="Transparent" ToolTipService.ToolTip="Виконано">
                                        <SymbolIcon Symbol="Accept" Foreground="LightGreen"/>
                                    </Button>
                                    <Button x:ConnectionId='20'                          Background="Transparent" ToolTipService.ToolTip="Пропущено">
                                        <SymbolIcon Symbol="Cancel" Foreground="#D15A5A"/>
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <Button x:ConnectionId='17' Grid.Row="1" x:Name="CompleteDayBtn" Content="Завершити тренувальний день" Visibility="Collapsed"                              HorizontalAlignment="Stretch" Margin="0,15,15,0" Padding="15" Style="{StaticResource AccentButtonStyle}"/>
        </Grid>

        <ContentDialog x:ConnectionId='3' x:Name="SetupGoalDialog"
                   Title="Налаштування тренувальної цілі"
                   PrimaryButtonText="Сформувати розклад"
                   IsPrimaryButtonEnabled="False"
                                                                          >
            <StackPanel Spacing="15" Width="350">
                <ComboBox x:ConnectionId='4' x:Name="GoalComboBox" Header="Оберіть мету" HorizontalAlignment="Stretch" SelectedIndex="0">
                    <ComboBoxItem Content="Схуднення та рельєф" Tag="WeightLoss"/>
                    <ComboBoxItem Content="Набір м'язової маси" Tag="MuscleGain"/>
                    <ComboBoxItem Content="Підтримка форми" Tag="KeepFit"/>
                    <ComboBoxItem Content="Розвиток сили" Tag="Strength"/>
                    <ComboBoxItem Content="Здоров'я та гнучкість" Tag="HealthAndMobility"/>
                </ComboBox>

                <TextBlock Text="Рівень навантаження (1 - Легко, 5 - Важко)"/>
                <Slider x:ConnectionId='5' x:Name="IntensitySlider" Minimum="1" Maximum="5" Value="3" TickFrequency="1" TickPlacement="BottomRight"/>

                <TextBlock x:ConnectionId='6' x:Name="DaysRequirementText" Text="Оберіть дні тренувань:" FontWeight="SemiBold"/>
                <StackPanel x:ConnectionId='7' x:Name="DaysTogglePanel" Orientation="Horizontal" Spacing="5" HorizontalAlignment="Center">
                    <ToggleButton x:ConnectionId='9' Content="Пн" Tag="Monday"                         Width="40"/>
                    <ToggleButton x:ConnectionId='10' Content="Вт" Tag="Tuesday"                         Width="40"/>
                    <ToggleButton x:ConnectionId='11' Content="Ср" Tag="Wednesday"                         Width="40"/>
                    <ToggleButton x:ConnectionId='12' Content="Чт" Tag="Thursday"                         Width="40"/>
                    <ToggleButton x:ConnectionId='13' Content="Пт" Tag="Friday"                         Width="40"/>
                    <ToggleButton x:ConnectionId='14' Content="Сб" Tag="Saturday"                         Width="40"/>
                    <ToggleButton x:ConnectionId='15' Content="Нд" Tag="Sunday"                         Width="40"/>
                </StackPanel>
                <TextBlock x:ConnectionId='8' x:Name="DaysWarningText" Text="" Foreground="#D15A5A" FontSize="12" TextWrapping="Wrap"/>
            </StackPanel>
        </ContentDialog>
    </Grid>
</Page>

